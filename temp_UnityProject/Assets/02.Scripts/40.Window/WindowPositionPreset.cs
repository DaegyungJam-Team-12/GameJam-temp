#nullable enable

namespace Icebreaker.Window
{
    /// <summary>Position presets, relative to the OS work area of the current monitor.</summary>
    public enum WindowPositionPreset
    {
        BottomLeft,
        BottomCenter,
        BottomRight
    }
}
